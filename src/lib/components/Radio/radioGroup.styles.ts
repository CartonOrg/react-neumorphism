import { Sizes, spacings } from "../../constants";

export const RADIO_GROUP_STYLE: Record<Sizes, React.CSSProperties> = {
  xs: {
    gap: spacings.xs,
  },
  sm: {
    gap: spacings.sm,
  },
  md: {
    gap: spacings.sm,
  },
  lg: {
    gap: spacings.sm,
  },
  xl: {
    gap: spacings.sm,
  },
};
